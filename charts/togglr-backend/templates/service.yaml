apiVersion: v1
kind: Service
metadata:
  name: {{ include "togglr-backend.fullname" . }}
  labels:
    {{- include "togglr-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "togglr-backend.selectorLabels" . | nindent 4 }}