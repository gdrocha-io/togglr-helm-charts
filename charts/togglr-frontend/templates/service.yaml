apiVersion: v1
kind: Service
metadata:
  name: {{ include "togglr-frontend.fullname" . }}
  labels:
    {{- include "togglr-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "togglr-frontend.selectorLabels" . | nindent 4 }}